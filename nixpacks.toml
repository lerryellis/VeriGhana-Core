[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["python3 -m venv /app/.venv", "/app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/uvicorn src.api:app --host 0.0.0.0 --port $PORT"
